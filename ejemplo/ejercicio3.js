let ocupa = true 
let nomlocal = 'markini'

switch (ocupa) {
    
    case  true:
        console.log(`el local ${nomlocal} se encuentra disponible`)        
        break;

    default:
        console.log(`el local ${nomlocal} no se encuentra disponible`)
        break;
}