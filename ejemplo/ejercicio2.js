let num = parseInt(prompt('ingrese un numero para determinar su es negativo o positivo'))

switch (num ) {
    case  num >=0 :
        alert('su numero es positivo')   
        break;
    default:
        alert('su numero es negativo')
        break;
}