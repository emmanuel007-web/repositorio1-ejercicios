function arrendador(ocupacionLocal) {
    let nombrelocal = "markini"

    if (ocupacionLocal == true ){
        console.log (`el local ${nombrelocal} se encuentra arrendado`)
    }else{
        console.log(`el local ${nombrelocal} se encuentra disponible`)
    }

} 

console.log(arrendador(true))